@model MissionAuthen.Models.Question

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3> @*shows informaiton for question*@
<div>
    <h4>Question</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MissionId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MissionId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.QuestionDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.QuestionDescription)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> | @*submits form to httppost delete action method which deletes question*@
            @Html.ActionLink("Back to List", "Index") @*returns to index instead of deleting question*@
        </div>
    }
</div>
